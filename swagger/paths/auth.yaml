  ## Auth
/auth/login/addAdmin:
    get:
      summary: Adminni yaratish.
      description: Optional extended description in CommonMark or HTML.
      responses:
        "200": # status code
          description: A JSON array of user names
          content:
            application/json:
              schema:
                type: object
                items:
                  type: string
      tags:
        - Auth
/auth/login:
    post:
      summary: login.
      description: Optional extended description in CommonMark or HTML.
      requestBody:
        description: Optional description in *Markdown*
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/login"
      responses:
        "200": # status code
          description: A JSON array of user names
          content:
            application/json:
              schema:
                type: object
                items:
                  $ref: "#/components/schemas/login"
      tags:
        - Auth

/auth/checkuser:
    get:
      summary: userni  tekshirish layout uchun.
      security:
        - BearerAuth: [ ]
      responses:
        "200": # status code
          description: A JSON array of user names
          content:
            application/json:
              schema:
                type: object
                items:
                  type: string
      tags:
        - Auth